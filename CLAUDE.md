#  AI 영상 공모전 워크스페이스 운영 규칙

## 목적

이 저장소는 두 사람이 함께 만드는 AI 숏드라마의 프로젝트 기억이자 Codex 멀티에이전트 작업 공간이다. 일반 영화학 백과사전이 아니라 인간의 결정, 결정 근거, 미결 질문, 폐기된 아이디어, 컷 정의, 실제 생성 결과를 보존한다.

항상 한국어로 응답한다. 사용자가 명시적으로 다른 언어를 요청한 경우만 예외로 한다.

## 작업 시작 순서

모든 에이전트는 필요한 범위만 다음 순서로 읽는다.

1. `HOME.md`
2. `project/PROJECT_BRIEF.md`
3. `project/CANON.md`와 `project/OPEN_QUESTIONS.md`
4. 작업과 관련된 `project/decisions/` 기록과 `knowledge/CONTEST_RULES.md`
5. 관련 Shot, 인접 Shot, 최근 Run

전체 Vault를 무조건 읽지 않는다. 관련 문서가 없거나 서로 충돌하면 추측하지 말고 `정보 없음` 또는 `동기화 필요`로 보고한다.

## 지식의 권위와 상태

| 지식 종류 | 권위 위치 | 취급 방식 |
|---|---|---|
| 프로젝트 목적·팀·운영 제약 | `project/PROJECT_BRIEF.md` | 현재 확정 사실 |
| 스토리·캐릭터·세계·연출의 현재 확정 내용 | `project/CANON.md` | 인간이 명시적으로 승인한 내용만 |
| 결정과 근거 | `project/decisions/DEC-*.md` | `accepted`만 현재 결정의 근거 |
| 미결 사항 | `project/OPEN_QUESTIONS.md` | 확정 사실로 사용 금지 |
| 회의 기록 | `project/meetings/MTG-*.md` | 역사적 증거이며 그 자체로 확정 아님 |
| 제안·폐기 아이디어 | `project/IDEA_REGISTER.md` 및 필요 시 개별 Idea | Canon으로 사용 금지 |
| 외부 규정 | `knowledge/CONTEST_RULES.md` | 출처·확인일·검증 수준과 함께 사용 |
| 현재 컷 정의·승인 프롬프트 | `production/shots/SH-*.md` | 해당 Shot의 단일 원본 |
| 실제 생성 입력·출력·평가 | `production/runs/RUN-*.md` | 실행 증거. 정확한 입력은 사후 수정 금지 |
| 반복 검증된 프로젝트 제작 지식 | `knowledge/PROJECT_PRACTICES.md` | Run 근거와 인간 승인이 있을 때만 승격 |

지식은 다음 네 종류를 섞지 않는다.

- 확정된 인간 결정
- 검증된 외부 사실
- 생성 결과에서 직접 관찰한 사실
- 가설·해석·제안

회의록, 에이전트 제안, 한 번의 생성 결과만으로 Canon을 갱신하지 않는다. 충돌 시 임의로 하나를 선택하지 말고 출처와 충돌 내용을 제시한다.

## 창작 사실 변경 규칙

- 아직 정하지 않은 스토리, 캐릭터, 세계관, 연출 방향, 설정을 빈칸 보완 목적으로 만들지 않는다.
- 에이전트가 새로 제시하는 창작 내용은 기본적으로 `proposed`다.
- 인간이 명시적으로 승인한 경우에만 `DEC-*` 기록을 `accepted`로 만들고, 같은 작업에서 `CANON.md` 또는 `PROJECT_BRIEF.md`를 갱신한다.
- 기존 기록을 삭제하거나 과거 결론을 고쳐 쓰지 않는다. 새 Decision에서 `supersedes`로 연결한다.
- 폐기 아이디어와 실패 Run은 재논의 방지와 학습을 위해 보존한다.

## 파일 편집 전 필수 확인

편집 전에 다음을 사용자에게 간결히 밝힌다.

- 목표
- 범위
- 수정 허용 파일
- 수정 금지 파일
- 성공 기준
- 중단 조건

작고 검토 가능한 변경을 우선한다. 요청 범위 밖의 파일과 `.obsidian/` 개인 UI 설정을 수정하지 않는다.

다음 작업 전에는 확인을 받는다.

- 의존성 추가
- 파일 삭제나 파괴적 명령
- 핵심 구조 변경
- 외부 플랫폼에서 비용이 드는 생성, 공개 업로드, 제출
- 릴리스, 배포, 병합, 프로덕션 작업

## 멀티에이전트 운영 계약

Orchestrator는 고정된 에이전트 조직을 유지하지 않고 Shot의 위험에 맞는 검토 관점을 동적으로 위임한다. 기본 검토 범위는 다음과 같다.

1. 서사적 기능, 연출, 연기
2. 카메라, 구도, 미장센, 조명
3. 편집, 공간·시선·동작 연속성
4. AI 생성 가능성, 인물·의상·소품·환경 consistency

작업이 작으면 관점을 묶을 수 있다. 전문 에이전트는 기본적으로 읽기 전용이며, Orchestrator 한 명이 결과를 종합해 Shot과 인덱스를 수정한다. 같은 파일을 여러 에이전트가 병렬 편집하지 않는다.

위임 패킷에는 반드시 다음이 포함된다.

- 목표와 Shot ID
- 읽어야 할 Wikilink
- 잠긴 사실과 제약
- 답해야 할 질문
- 출력 형식
- 수정 허용 파일
- 중단 조건

서브에이전트 응답은 `확인한 제약 / 관찰 / 위험 / 권고와 대안 / 가정 / 충돌 / 미결 질문 / verdict(pass|revise|block)`를 구분한다. Canon과 규정에 없는 창작 사실이 필요하면 `block` 또는 명시적 제안으로 반환한다.

상세 제작 흐름과 상태 전이는 `production/PIPELINE.md`를 따른다.

## 프롬프트와 생성 기록

- Shot에는 현재 승인 후보 또는 승인된 프롬프트만 둔다.
- 실제 생성에 사용한 정확한 프롬프트, 설정, 모델·버전, 참조 입력은 각 Run에 스냅샷으로 보존한다.
- 재시도는 기존 Run을 덮어쓰지 않고 새 Run ID로 기록한다.
- 실패 원인은 관찰과 가설을 분리하고 확신도를 적는다.
- 개선 실험은 가능하면 한 번에 한 가지 핵심 변수를 바꾼다.
- 일반 영화·영상 지식은 Wiki에 축적하지 않는다. 이 프로젝트에서 반복 사용하거나 Run으로 검증된 특수 원칙만 저장한다.

## 외부 정보 규칙

- 규정과 플랫폼 정보에는 원문 URL과 확인일을 기록한다.
- 원문, 2차 출처, 팀의 해석을 구분한다.
- 변할 수 있는 정보는 사용 직전에 다시 확인한다.
- 확인하지 못한 모델명, 버전, 설정, 권리 조건을 추정하지 않는다.

## 파일명과 ID

- 회의: `project/meetings/MTG-YYYYMMDD-주제.md`
- 결정: `project/decisions/DEC-001-주제.md`
- Shot: `production/shots/SH-001-주제.md`
- Run: `production/runs/RUN-SH-001-001.md`

ID는 정렬 순서가 바뀌어도 재사용하거나 변경하지 않는다.

## 검증과 보고

- 실행하지 않은 테스트를 통과했다고 말하지 않는다.
- 변경 후에는 변경 파일, 요약, 실행한 테스트, 알려진 위험, 수동 확인 항목, 다음 행동을 보고한다.
- 여러 단계의 설정·개발 작업 응답 끝에는 다음 항목의 Resume Block을 포함한다: 현재 단계, 완료된 단계, 현재 중단점, 다음 정확한 작업, 다음 명령 또는 프롬프트, 변경·검토 예상 파일, 중단 조건.

